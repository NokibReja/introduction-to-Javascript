const isTrue = true;
// if (isTrue ==== true)
if (isTrue) {
    console.log("you are winner");
}

const isPassed = false;
// if (isPassed === false)
if (!isPassed) {
    console.log("you are loser");
}